enum ThemeMode {
  System,
  Light,
  Dark,
}

enum VideoFormat { MOV, AVI, MKV, MP4, FLV, WMV, RMVB, _3GP, MPG, MPE, M4V }

enum ExecuteStatus {
  Idle,
  Executing,
  Success,
  Failed,
  Canceled,
  Paused,
  Stopped,
  Waiting,
  Completed,
  Unknown,
}
